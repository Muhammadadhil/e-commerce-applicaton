<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>OTP Verification</title>
  <!-- Bootstrap CSS -->
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Custom styles */
    .otp-input {
      width: 50px;
      text-align: center;
      margin: 0 5px;
      padding: 10px;
      font-size: 18px;
    }
  </style>
</head>
<body>

<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">Enter OTP</h5>
          <form id="otpForm">
            <div class="form-group">
              <div class="d-flex justify-content-center">
                <!-- OTP input fields -->
                <input type="text" class="form-control otp-input" maxlength="1" name="digit1" id="digit1" pattern="[0-9]{1}" required>
                <input type="text" class="form-control otp-input" maxlength="1" name="digit2" id="digit2" pattern="[0-9]{1}" required>
                <input type="text" class="form-control otp-input" maxlength="1" name="digit3" id="digit3" pattern="[0-9]{1}" required>
                <input type="text" class="form-control otp-input" maxlength="1" name="digit4" id="digit4" pattern="[0-9]{1}" required>
              </div>
            </div>
            <div class="form-group">
              <button type="submit" class="btn btn-primary btn-block">Verify OTP</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Bootstrap JS (optional, for certain components) -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
